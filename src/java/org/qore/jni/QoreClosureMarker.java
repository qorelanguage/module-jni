package org.qore.jni;

//! This marks a class that must have a \c call() method (with any argument and return types) that will be called when this object is converted to a Qore closure and the closure is called
public interface QoreClosureMarker {
}
